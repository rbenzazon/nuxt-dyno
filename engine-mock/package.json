{
	"name": "engine-mock",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "node engine-mock.mjs",
		"dev": "nodemon --verbose --watch . --exec \"node\" engine-mock.mjs",
		"pretty": "prettier --write .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"express": "^5.2.1",
		"ws": "^8.18.3"
	},
	"devDependencies": {
		"nodemon": "^3.1.11",
		"prettier": "^3.7.4"
	}
}
